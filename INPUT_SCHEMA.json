{
  "title": "DNB Scraper (Firebase optional)",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "editor": "requestListSources",
      "description": "List of page sources to crawl. Use objects like { \"url\": \"https://...\" }."
    },
    "maxRequestsPerCrawl": {
      "title": "Max requests",
      "type": "integer",
      "default": 50,
      "description": "Upper bound for total requests in a single run."
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "default": 5,
      "description": "Maximum parallel pages to open at once."
    },
    "maxRequestsPerMinute": {
      "title": "Throttle (requests/min)",
      "type": "integer",
      "default": 60,
      "description": "Rate limit to be polite and reduce blocks."
    },
    "useProxy": {
      "title": "Use Apify Proxy",
      "type": "boolean",
      "default": false,
      "description": "Toggle to route traffic via Apify Proxy."
    },
    "proxyGroups": {
      "title": "Proxy groups",
      "type": "array",
      "editor": "stringList",
      "description": "Optional Apify proxy groups (e.g., SHADER, BUYPROXIES94952)."
    },
    "selectors": {
      "title": "CSS selectors",
      "type": "object",
      "description": "Configure CSS selectors for fields you want to extract.",
      "properties": {
        "name": { "type": "string", "default": "h1.company-name", "description": "Selector for company name." },
        "address": { "type": "string", "default": "p.company-address", "description": "Selector for street address." },
        "phone": { "type": "string", "default": "a.company-phone", "description": "Selector for phone number." },
        "website": { "type": "string", "default": "a.company-website", "description": "Selector for the company website link (href)." },
        "industry": { "type": "string", "default": "span.company-industry", "description": "Selector for industry/category label." }
      }
    },
    "firebaseEnabled": {
      "title": "Enable Firebase sync",
      "type": "boolean",
      "default": false,
      "description": "If true, upsert results to Firestore in addition to the dataset."
    },
    "firebaseCollection": {
      "title": "Firestore collection",
      "type": "string",
      "default": "companies",
      "description": "Firestore collection name for saved documents."
    },
    "login": {
      "title": "(Optional) Login flow — only use with permission",
      "type": "object",
      "description": "Configure a simple form-based login step. Only use if the site permits it.",
      "properties": {
        "enabled": { "type": "boolean", "default": false, "description": "Enable the login step before crawling." },
        "loginUrl": { "type": "string", "description": "URL of the login page." },
        "username": { "type": "string", "description": "Login username (use Apify input secret fields for sensitive data)." },
        "password": { "type": "string", "description": "Login password (use Apify input secret fields for sensitive data)." },
        "usernameSelector": { "type": "string", "default": "input[name=email]", "description": "CSS selector for username/email input." },
        "passwordSelector": { "type": "string", "default": "input[type=password]", "description": "CSS selector for password input." },
        "submitSelector": { "type": "string", "default": "button[type=submit]", "description": "CSS selector for submit button." }
      }
    }
  },
  "required": ["startUrls"]
}
